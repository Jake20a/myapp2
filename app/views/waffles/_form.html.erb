<%= form_with(model: waffle, local: true) do |form| %>
  <% if waffle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(waffle.errors.count, "error") %> prohibited this waffle from being saved:</h2>

      <ul>
      <% waffle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :waffle_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
